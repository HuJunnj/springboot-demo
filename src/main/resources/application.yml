server:
  port: 8080
  servlet:
    context-path: /cms
spring:
  profiles:
    active: dev # 默认启用开发环境
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
